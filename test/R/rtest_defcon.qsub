#!/bin/bash -l

# === Generated by defcon stage1 — submit this script ===

#$ -pe omp 1
#$ -l h_rt=0:10:00

# Capture environment before modules alter it
env -0 > $TMPDIR/defcon_env.out

# Run original script under strace
strace -f -e trace=file -s 4096 -o $TMPDIR/defcon_trace.out bash rtest_defcon.run.sh

# Automatically generate Singularity definition (DEFCON 1)
python3 /projectnb/rcs-intern/reetom/defcon-singularity/defcon.py stage2 -t $TMPDIR/defcon_trace.out -e $TMPDIR/defcon_env.out --command-file rtest_defcon.run.sh -o rtest_defcon.def
