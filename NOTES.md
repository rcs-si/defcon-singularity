### Brainstorming ideas on how this might work in practice. 

```
defcon -stage1 -i input.qsub -inc_dir /opt/yadda  -opt2 -o output.qsub
```

```
**** INPUT QSUB ****
#!/bin/bash -l

#$ -pe omp 4

module load python3/3.13.8
python myscript.py -in x -out y
*******************
```

```
# Generated by defcon 
**** Output runscript output.sh ****
echo --------------------------------
printenv 
echo --------------------------------

module load python3/3.13.8
python myscript.py -in x -out y

echo --------------------------------
printenv
echo --------------------------------
*******************
```

```
# Generated by defcon 
**** OUTPUT QSUB output.qsub ****
#!/bin/bash -l
#$ -pe omp 4 

strace -f output.sh -o $TMPDIR/strace.out

defcon -stage2 -strace_in $TMPDIR/strace.out -def_out output.def
```

# User then does:
qsub output.qsub 

